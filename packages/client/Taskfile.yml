version: '3'

tasks:
  shadcn:add:
    desc: add shadcn component
    cmds:
      - pnpm dlx shadcn-ui@latest add {{ .name }} -c ./packages/client

  graphql:gen:
    desc: generate graphql code
    cmds:
      - ./node_modules/.bin/graphql-codegen --config ./packages/client/codegen.ts
